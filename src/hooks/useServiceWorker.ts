// Service worker completely disabled.
// All registration, update checks, and caching have been removed.
const useServiceWorker = () => {
  // No-op — service worker intentionally deactivated.
};

export default useServiceWorker;
